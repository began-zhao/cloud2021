server:
  port: 8002
spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: com.mysql.jdbc.Driver         # mysql驱动包 com.mysql.jdbc.Driver
    url: jdbc:mysql://120.25.165.167:3306/db01?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: begin123456
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.begin.springcloud.entities    # 所有Entity别名类所在包

#eureka配置
eureka:
  client:
    #表示是否将自己注册进EurekaServer默认为true。
    register-with-eureka: true
    #是否从EurekaServer抓取已有的注册信息，默认为true。单节点无所谓，集群必须设置为true才能配合ribbon使用负载均衡
    fetchRegistry: true
    service-url:
      #指定入驻的主机和端口
      #      defaultZone: http://localhost:7001/eureka   #单机版配置
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka #集群版
  instance:
    instance-id: payment8002
    prefer-ip-address: true




